utils::globalVariables(c(
  # Plotting and aesthetics
  "..density..", "pcolor", "X", "Y", "X1", "X2",

  # Data columns and variables
  "cluster", "votes", "Turnout", "Support", "colors.v",

  # Function parameters and internal variables
  "precinctLevel", "iterations", "itstartMAX", "z", "j",

  # Package availability flags
  "mixtools_available", "dbscan_available",

  # Common statistical variables
  "value", "variable", "id", "region"
))
